# Messaggio di avvio suggerito per la nuova sessione

Aprire Claude Code nella cartella `~/Coding/Profumari consulente` e incollare:

> Questo è il kit di avvio del progetto "Profumari · Consulente olfattivo", preparato nella sessione del gestionale il 16/09/2026. Leggi `CLAUDE.md` e poi i documenti in `docs/` in ordine. Poi esegui il "Primo compito" descritto in `CLAUDE.md`: inizializza il repository, crea lo scheletro dell'app con la veste grafica di `docs/07-stile.md`, scrivi il motore con i test di `docs/04-motore.md` prima delle schermate, e costruisci il percorso di `docs/05-percorso.md` usando `dati/catalogo.json` e `dati/profili.json`. Ricorda le regole non negoziabili: mai nomi commerciali, nessun collegamento col gestionale, solo referenze attive.

Cose da avere sotto mano:

- Account GitHub `alexpax85` per creare la repo `profumari-consulente` (`gh repo create`), poi attivare GitHub Pages con sorgente "GitHub Actions".
- Le risposte del cliente alle domande di `docs/08-domande-cliente.md`, anche parziali.
- L'export ufficiale del catalogo dal gestionale, quando esisterà il pulsante (vedi `docs/03-dati.md`); fino ad allora si usa `dati/catalogo.json`.
